package com.kpstv.jettweet.ui.utils

import com.kpstv.jettweet.R

enum class Screen(val id: Int) {
    HOME(id = R.id.home_fragment),
    SEARCH(id = R.id.search_fragment),
    NOTIFY(id = R.id.home_fragment),
    MAIL(id = R.id.home_fragment)
}